[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm ci --production=false", "cd frontend && npm ci --production=false"]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "npm start"
